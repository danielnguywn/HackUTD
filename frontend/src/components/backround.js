import React from 'react';
import Box from '@mui/material/Box';

const Background = () => {
     return (
          <Box sx={{ position: 'absolute', inset: 0, background: 'linear-gradient(to bottom, #7579E1, #40B7BA)' }} />
     );
};

export default Background;
